from django.db import models

# Create your models here.
# class User(auth.models.User):
#     designation=models.CharField(max_length=50)
#     PLAN_CHOICES = (
#     ('startup', 'Startup'),
#     ('pro','Pro'),
#     ('enterprise','Enterprise'),
#     )
#     plan = models.CharField(max_length = 12,choices=PLAN_CHOICES,default='startup')
#     company_email=models.EmailField(max_length=100)
#     credits_bought=models.IntegerField(default=0)
#     last_plan_bought=models.DateTimeField(auto_now_add=True)
#     last_credits_bought=models.DateTimeField(auto_now_add=True)
#     plan_order_id=models.CharField(max_length=50)    
#     is_plan_paid=models.BooleanField(default=False) #during payment, check if payment successful   
#     credit_order_id=models.CharField(max_length=50)    
#     is_credit_paid=models.BooleanField(default=False) #during payment, check if payment successful  
#     top_reqs=models.IntegerField(default=0)
#     topic_reqs=models.IntegerField(default=0)
#     query_reqs=models.IntegerField(default=0)
#     top_articles=models.IntegerField(default=0)
#     topic_articles=models.IntegerField(default=0)
#     query_articles=models.IntegerField(default=0)

